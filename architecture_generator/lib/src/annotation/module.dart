const Object ApplicationModule = const $ApplicationModule._();

class $ApplicationModule {
  const $ApplicationModule._();
}

class ScreenProvider {
  final Type viewModel;

  const ScreenProvider({this.viewModel}) : assert(viewModel != null);
}
